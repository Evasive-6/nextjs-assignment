{
  "name": "nextjs-env-aware-builds",
  "version": "1.0.0",
  "description": "Next.js with environment-aware builds and secrets management",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:development": "NODE_ENV=development next build",
    "build:staging": "NODE_ENV=staging next build",
    "build:production": "NODE_ENV=production next build",
    "start": "next start",
    "lint": "next lint",
    "test": "jest"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "next": "^15.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-next": "^15.0.0",
    "@testing-library/react": "^14.0.0",
    "@testing-library/jest-dom": "^6.0.0",
    "jest": "^29.0.0",
    "@babel/preset-env": "^7.20.0",
    "@babel/preset-react": "^7.18.0"
  }
}
